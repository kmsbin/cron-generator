name: cron_generator
publish_to: pub.dev

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  analyzer: '>=5.2.0 <7.0.0'
  build: ^2.0.0
  collection: ^1.18.0
  cron: any
  meta: ^1.12.0
  source_gen: any

dev_dependencies:
  dart_flutter_team_lints: ^2.1.1

dependency_overrides:
  source_gen:
    path: ../source_gen
